.gradle/
build/
/local.properties
.idea/
*.iml
.DS_Store
captures/

# Private legal documents (contracts, mandates) — never publish
private/

# Secrets / config that must NOT go in a public repo
app/google-services.json
*.jks
*.keystore
keystore.properties
key.properties
release-keystore-info.txt
