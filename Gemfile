source "https://rubygems.org"

# fastlane drives Play Store uploads (see fastlane/). Requires Ruby >= 2.7;
# macOS system Ruby (2.6) is too old — install a newer Ruby first (see fastlane/SETUP.md).
gem "fastlane"
